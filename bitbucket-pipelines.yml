image: node:8.9.0-stretch
pipelines:
  default:
    - step:
        caches:
          - node
        script:
          - make install
          - make lint

  branches:
    '{develop,staging,master}':
        - step:
            script:
              - echo "No pipeline for develop, staging, master"
