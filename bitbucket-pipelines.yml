image: node:8.9.0-stretch
pipelines:
  default:
    - parallel:
      - step:
          caches:
            - node
          script:
            - make install
      - step:
          script:
            - make lint
  branches:
    '{develop,staging,master}':
        - step:
            script:
              - echo "No pipline for develop, staging, master"