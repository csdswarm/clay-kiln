This is a template file. It can be pretty much any type of text file.

If you need to output a handlebars file, escape the handlebars output items using \\

e.g. \{{ someValue }} instead of \{{ someValue }}

Value from list: \{{ valueFromList }}
Value from rawList: \{{ valueFromRawList }}
Value from expand: \{{valueFromExpand}}
Values from checkBox:
\{{#each valueFromCheckbox}}
  - \{{ . }}
\{{/each}}
Value from confirm: \{{valueFromConfirm}}
Value from input: \{{ valueFromInput }}
Value from number: \{{valueFromNumber}}
Value from password: \{{{valueFromPassword}}}
Valur from editor: \{{{valueFromEditor}}}
