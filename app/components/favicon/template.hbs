{{!-- Favicons & Apple Touch Icons --}}
{{#each _computed.icons as |icon|}}
  <link
    rel="{{ icon.rel }}"
    {{#if icon.type}}
      type="{{ icon.type }}"
    {{/if}}
    {{#if icon.sizes}}
      sizes="{{ icon.sizes }}"
    {{/if}}
    href="{{ icon.href }}"
  >
{{/each}}