<header
  data-uri="{{ default _ref _self }}"
  class="component component--topic-page-header {{ if @root.locals.edit ' editing' }}"
  data-editable="settings">
  {{#if author}}
    {{ author }}
  {{/if}}
</header>
