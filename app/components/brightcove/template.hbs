<div data-uri="{{ default _ref _self }}" class="component component--brightcove" data-editable="videoSettings">
  <div class="player">
    {{#if video.id }}
      <div class="player__video {{#unless showTitle}}player__video--hidden-title{{/unless}}">
        {{#unless @root.locals.edit}}
          <video-js id="video-{{ video.id }}"
            class="video-js vjs-16-9"
            data-video-id="{{ video.id }}"
            data-account="{{ @root.locals.site.brightcoveAccountId }}"
            data-player="{{ @root.locals.site.brightcovePlayerId }}"
            data-embed="default"
            data-application-id
            controls></video-js>
        {{else}}
          <div class="video__preview" style="min-height: 20px; margin-bottom: 20px;">
            <div>
              {{#if video.name}}<span>{{ video.name }}</span>{{/if}}
              <span>Brightcove ID: {{ video.id }}</span>
            </div>
            {{#if video.imageUrl}}<img src="{{ video.imageUrl }}" alt="{{ video.name }}">{{/if}}
          </div>
        {{/unless}}
      </div>
    {{/if}}
  </div>
</div>
