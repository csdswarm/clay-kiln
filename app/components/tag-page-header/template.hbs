<header
  data-uri="{{ default _ref _self }}"
  class="component component--tag-page-header tag-page-header--{{tag}}
    {{ if @root.locals.edit ' editing' }}
    {{ if image ' tag-page-header--image'}}
  "
  {{#if image}}
    style="background-image:url({{ image }});"
  {{/if}}
>
  <div class="tag-page-header__tag" data-editable="settings">
    <div class="tag__line"></div>
    {{#if dynamicTag}}
      {{ set 'displayTag' (replace tag '-' ' ') }}
      <h1>{{displayTag}}</h1>
    {{else}}
      <h1>{{tag}}</h1>
    {{/if}}
  </div>
</header>
