contentLabel:
  _label: Lede Label
  _placeholder:
    text: Lede Label
    height: 30px;
    required: true
  _has:
    input: text
    validate:
      max: 80
    help: Label for the lede event. Eg. 'Featured Event'

lede:
  _subscribe: lede

# Component properties
description:
  _component:
    include:
      - paragraph

googleAdRightRail:
  _component:
    include:
      - google-ad-manager

_groups:
  settings:
    fields:
      - contentLabel
