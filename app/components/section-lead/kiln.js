'use strict';

const KilnInput = window.kiln.kilnInput;

module.exports = schema => {
  // TODO: handle setting max length of items to 2 when in multi-column mode and hide content label when it's not
  return schema;
};
