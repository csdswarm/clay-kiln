'use strict';

const
  chai = require('chai'),
  sinon = require('sinon'),
  sinonChai = require('sinon-chai');
  // apMedia = require('./ap-media'),
  // { expect } = chai;

chai.use(sinonChai);

describe('server', () => {
  afterEach(sinon.restore);

  describe('ap-media', () => {
    // function setupApMedia() {
    //   return apMedia;
    // }
    
    describe('searchAp', () => {

    });

    describe('getApFeed', () => {
      
    });

    describe('saveApPicture', () => {

    });

    describe('getApArticleBody', () => {
      // async function setup_getArticleBody(options = {}) {
      //   const setup = setupApMedia(),
      //     { getApArticleBody } = setup;

      // }
    });
  });
});
