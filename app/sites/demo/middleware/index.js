'use strict';

const { episode } = require('./episode'),
  { podcast } = require('./podcast'),
  { dynamicPage } = require('./dynamicPage');

module.exports = {
  episode,
  podcast,
  dynamicPage
};
